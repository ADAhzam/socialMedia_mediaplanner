# Team gate (set the same in Vercel project env). Leave unset for open local dev.
TEAM_USER=joveo
TEAM_PASS=change-me
# Set ALLOW_UNAUTHENTICATED=1 to run open locally without credentials (never in production).
ALLOW_UNAUTHENTICATED=1
